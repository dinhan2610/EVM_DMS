# 🎨 VISUAL FLOW - 2 CÁCH TẠO HÓA ĐƠN

**Component:** CreateVatInvoice.tsx  
**Date:** 19/01/2026

---

## 📊 FLOW 1: TẠO HÓA ĐƠN TRỰC TIẾP

```
┌──────────────────────────────────────────────────────────────┐
│                     USER (Accountant/Admin)                    │
└──────────────────────┬───────────────────────────────────────┘
                       │
                       ▼
              ┌────────────────┐
              │  Click "Tạo    │
              │   Hóa Đơn"     │
              └────────┬───────┘
                       │
                       ▼
         ┌─────────────────────────┐
         │  Navigate: /newinvoices │
         │  (không có ?requestId)  │
         └──────────┬──────────────┘
                    │
                    ▼
    ┌───────────────────────────────────┐
    │  State Detection:                 │
    │  - prefillRequestId = null        │
    │  - isPrefillMode = false          │
    │  - prefillSalesID = 0             │
    └──────────┬────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│  User nhập thông tin thủ công:         │
│  - Chọn template                        │
│  - Nhập/chọn khách hàng                 │
│  - Nhập sản phẩm                        │
│  - Nhập số lượng, đơn giá               │
└──────────┬──────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────┐
│  Click "Lưu nháp" hoặc "Gửi duyệt"      │
└──────────┬───────────────────────────────┘
           │
           ▼
┌────────────────────────────────────────────────┐
│  mapToBackendInvoiceRequest():                 │
│  ┌──────────────────────────────────────────┐ │
│  │  const performedByUser = currentUserId   │ │
│  │  const salesIDValue = undefined          │ │
│  │  const requestIDValue = null             │ │
│  └──────────────────────────────────────────┘ │
└──────────┬─────────────────────────────────────┘
           │
           ▼
┌────────────────────────────────────────────────┐
│  Backend Request Payload:                      │
│  ┌──────────────────────────────────────────┐ │
│  │  {                                       │ │
│  │    "templateID": 15,                     │ │
│  │    "customerID": 12,                     │ │
│  │    "performedBy": 10,                    │ │
│  │    // ❌ KHÔNG CÓ "salesID"              │ │
│  │    // ❌ KHÔNG CÓ "requestID"            │ │
│  │    "amount": 50420000,                   │ │
│  │    "taxAmount": 42000,                   │ │
│  │    "totalAmount": 50462000,              │ │
│  │    ...                                   │ │
│  │  }                                       │ │
│  └──────────────────────────────────────────┘ │
└──────────┬─────────────────────────────────────┘
           │
           ▼
    ┌─────────────────┐
    │  POST /api/     │
    │    Invoice      │
    └────────┬────────┘
             │
             ▼
┌────────────────────────────────────┐
│  Backend Response:                 │
│  - invoiceID: 206                  │
│  - requestID: null                 │
│  - salesID: null                   │
│  - invoiceNumber: 55               │
└────────────────────────────────────┘
```

---

## 📊 FLOW 2: TẠO HÓA ĐƠN TỪ INVOICE REQUEST

```
┌──────────────────────────────────────────────────────────────┐
│                        SALE USER                               │
└──────────────────────┬───────────────────────────────────────┘
                       │
                       ▼
              ┌────────────────┐
              │  Tạo Invoice   │
              │    Request     │
              │  (ID: 123)     │
              └────────┬───────┘
                       │
                       ▼
         ┌─────────────────────────┐
         │  POST /api/InvoiceRequest│
         │  salesID: 5 (Sale ID)   │
         └──────────┬──────────────┘
                    │
                    ▼
    ┌───────────────────────────────────┐
    │  Request saved in DB:             │
    │  - requestID: 123                 │
    │  - salesID: 5                     │
    │  - statusID: 1 (Pending)          │
    └──────────┬────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────────────────────────┐
│                   ACCOUNTANT/KTT USER                          │
└──────────────────────┬───────────────────────────────────────┘
                       │
                       ▼
              ┌────────────────────┐
              │  Xem danh sách     │
              │  Invoice Requests  │
              └────────┬───────────┘
                       │
                       ▼
         ┌─────────────────────────────┐
         │  Click "Tạo Hóa Đơn" từ    │
         │  request #123               │
         └──────────┬──────────────────┘
                    │
                    ▼
         ┌─────────────────────────────┐
         │  Navigate:                  │
         │  /newinvoices?requestId=123 │
         └──────────┬──────────────────┘
                    │
                    ▼
    ┌───────────────────────────────────┐
    │  State Detection:                 │
    │  - prefillRequestId = "123"       │
    │  - isPrefillMode = true           │
    └──────────┬────────────────────────┘
               │
               ▼
┌────────────────────────────────────────────────┐
│  useEffect - Load Prefill Data:               │
│  ┌──────────────────────────────────────────┐ │
│  │  GET /api/InvoiceRequest/123/prefill     │ │
│  │                                          │ │
│  │  Response:                               │ │
│  │  - customerID: 12                        │ │
│  │  - salesID: 5                           │ │
│  │  - items: [...]                         │ │
│  │  - paymentMethod: "Banking"             │ │
│  └──────────────────────────────────────────┘ │
└──────────┬─────────────────────────────────────┘
           │
           ▼
┌────────────────────────────────────────────────┐
│  Auto-fill Form:                               │
│  - setPrefillSalesID(5)                        │
│  - setBuyerCustomerID(12)                      │
│  - setItems([...])                             │
│  - setPaymentMethod("Chuyển khoản")            │
└──────────┬─────────────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────┐
│  KTT kiểm tra & điều chỉnh (nếu cần)   │
└──────────┬──────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────┐
│  Click "Lưu nháp" hoặc "Gửi duyệt"      │
└──────────┬───────────────────────────────┘
           │
           ▼
┌────────────────────────────────────────────────┐
│  mapToBackendInvoiceRequest():                 │
│  ┌──────────────────────────────────────────┐ │
│  │  const performedByUser = 10 (KTT)        │ │
│  │  const salesIDValue = 5 (from prefill)   │ │
│  │  const requestIDValue = 123              │ │
│  └──────────────────────────────────────────┘ │
└──────────┬─────────────────────────────────────┘
           │
           ▼
┌────────────────────────────────────────────────┐
│  Backend Request Payload:                      │
│  ┌──────────────────────────────────────────┐ │
│  │  {                                       │ │
│  │    "templateID": 15,                     │ │
│  │    "customerID": 12,                     │ │
│  │    "performedBy": 10,                    │ │
│  │    "salesID": 5,        // ✅ FROM REQUEST│ │
│  │    "requestID": 123,    // ✅ LINK       │ │
│  │    "amount": 50420000,                   │ │
│  │    "taxAmount": 42000,                   │ │
│  │    "totalAmount": 50462000,              │ │
│  │    ...                                   │ │
│  │  }                                       │ │
│  └──────────────────────────────────────────┘ │
└──────────┬─────────────────────────────────────┘
           │
           ▼
    ┌─────────────────┐
    │  POST /api/     │
    │    Invoice      │
    └────────┬────────┘
             │
             ▼
┌────────────────────────────────────────────────┐
│  Backend Processing:                           │
│  1. Tạo invoice mới (ID: 206)                  │
│  2. Link với request #123                      │
│     → UPDATE InvoiceRequest                    │
│       SET invoiceID = 206,                     │
│           statusID = 2 (Completed)             │
│  3. Lưu salesID = 5 cho báo cáo                │
└────────┬───────────────────────────────────────┘
         │
         ▼
┌────────────────────────────────────┐
│  Backend Response:                 │
│  - invoiceID: 206                  │
│  - requestID: 123      // ✅       │
│  - salesID: 5          // ✅       │
│  - invoiceNumber: 55               │
└────────────────────────────────────┘
```

---

## 🔍 KEY DIFFERENCES TABLE

| Aspect | Tạo Trực Tiếp | Tạo Từ Request |
|--------|---------------|----------------|
| **URL** | `/newinvoices` | `/newinvoices?requestId=123` |
| **Data Entry** | Nhập thủ công | Auto-fill từ request |
| **performedBy** | currentUserId (KTT) | currentUserId (KTT) |
| **salesID** | ❌ undefined | ✅ 5 (Sale tạo request) |
| **requestID** | ❌ null | ✅ 123 (Link) |
| **Backend Action** | Tạo invoice độc lập | Tạo + link + update request |
| **Request Status** | N/A | Pending → Completed |

---

## 💡 CODE COMPARISON

### **Logic Detection:**
```typescript
// Line 742-743
const prefillRequestId = searchParams.get('requestId')
const isPrefillMode = !!prefillRequestId

// Tạo trực tiếp:  prefillRequestId = null  → isPrefillMode = false
// Tạo từ request: prefillRequestId = "123" → isPrefillMode = true
```

### **Prepare Values:**
```typescript
// Line 1740-1752
const performedByUser = currentUserId;

const salesIDValue = isPrefillMode && prefillSalesID > 0 
  ? prefillSalesID 
  : undefined;
// Tạo trực tiếp:  salesIDValue = undefined
// Tạo từ request: salesIDValue = 5

const requestIDValue = isPrefillMode && prefillRequestId 
  ? parseInt(prefillRequestId) 
  : null;
// Tạo trực tiếp:  requestIDValue = null
// Tạo từ request: requestIDValue = 123
```

### **Adapter Logic:**
```typescript
// invoiceAdapter.ts - Line 340-365
const payload = {
  templateID,
  customerID,
  performedBy: signedBy,  // Always currentUserId
  // ... other required fields
};

// ✅ CHỈ thêm khi có giá trị
if (salesID !== undefined && salesID > 0) {
  Object.assign(payload, { salesID });
}

if (requestID !== null && requestID > 0) {
  Object.assign(payload, { requestID });
}

return payload;
```

---

## 🎯 RESULT COMPARISON

### **Database Records:**

**Tạo trực tiếp:**
```sql
-- invoices table
invoiceID | requestID | salesID | issuerID | ...
206       | NULL      | NULL    | 10       | ...
```

**Tạo từ request:**
```sql
-- invoices table
invoiceID | requestID | salesID | issuerID | ...
207       | 123       | 5       | 10       | ...

-- invoice_requests table
requestID | statusID | salesID | invoiceID | ...
123       | 2        | 5       | 207       | ...
```

---

## 🚀 USER EXPERIENCE

### **Tạo trực tiếp:**
```
1. KTT click "Tạo Hóa Đơn" ở menu
2. Màn hình trống, nhập thủ công
3. Submit → Invoice được tạo
4. Không liên kết với request nào
```

### **Tạo từ request:**
```
1. Sale tạo Invoice Request #123
2. KTT vào "Quản lý yêu cầu"
3. Tìm request #123, click "Tạo Hóa Đơn"
4. Màn hình auto-fill sẵn dữ liệu
5. KTT kiểm tra/sửa
6. Submit → Invoice tạo + link với request
7. Request #123 chuyển status → Completed
```

---

**✅ Clear visual flow giúp hiểu rõ hơn 2 modes!**
